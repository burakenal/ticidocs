# Full example

Guides + OpenAPI consumer-shaped Ticidocs project:

```text
examples/full/
├── content/{en,tr}/
├── openapi/openapi.yaml
└── docs.config.ts
```

Validates config, MDX, and OpenAPI loading. Closest to `apps/docs` content.
