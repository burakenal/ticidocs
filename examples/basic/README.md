# Basic example

Consumer-shaped Ticidocs project:

```text
examples/basic/
├── content/
│   ├── en/
│   └── tr/
├── docs.config.ts
└── public/   (optional assets)
```

This example validates config + MDX loading. Host it with `apps/docs` or scaffold a full Next site via `create-ticidocs`.
