# API example

Consumer-shaped Ticidocs project with OpenAPI navigation:

```text
examples/api/
├── content/{en,tr}/
├── openapi/openapi.yaml
└── docs.config.ts
```

Validates config, MDX, and OpenAPI loading.
